mixin panel-slider({ min= 0, max= 100, step= 1, orientation= 'horizontal', direction= 'ltr', from= 50, to, withRange= true, withThumb= true, withScale= true }={})
  .panel-slider(
    data-min= min
    data-max= max
    data-step= step
    data-orientation= orientation
    data-direction= direction
    data-from= from
    data-to= to
    data-with-range= `${withRange}`
    data-with-thumb= `${withThumb}`
    data-with-scale= `${withScale}`
  )